# Code Review Sprint1-4-main - PHP POO 1

**Exercise 1-1:**

- La lógica está bien, es correcta.
- Faltaría un mensaje cuando el sueldo no supera 6000.
- Ten en cuenta el nombre de las clases, tiene que seguir la convención **PascalCase**: en `employee` debería ser `Employee`.
- El método `data()` podría ser constructor `__construct()`.
- `print()` es una **palabra reservada de PHP**.

**Exercise 1-2:**

- El cálculo es correcto.
- Correcto método `__construct()` y herencia con el `extends`.
- Las clases se podrían separar en ficheros independientes.

**Exercise 2-1:**

- Todos los métodos están implementados.
- Los nombres de los métodos son descriptivos.

## Recomendaciones y comentarios

- Hay propiedades que podrían estar declaradas como `private` (encapsulación).
- Se podría añadir tipado a los métodos (`string`, `float`, `int`, `array`).
- También estaría bien añadir el fichero `README.md` en los ejercicios y así vas acostumbrándote (comentarlo con Rubén, a ver que opina).
- En general, está bien estructurado y métodos separados, la lógica es correcta y legible.
